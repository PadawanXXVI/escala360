# ============================================
# 🌐 ESCALA360 - Configuração de Ambiente
# ============================================

# -----------------------------
# 🔧 Flask Configuration
# -----------------------------
FLASK_APP=app.py
FLASK_ENV=development          # valores possíveis: development | production
FLASK_DEBUG=True               # ativa o modo debug local (False em produção)
SECRET_KEY=changeme123         # gere uma chave única e segura para produção

# -----------------------------
# 🌎 Server Configuration
# -----------------------------
HOST=127.0.0.1                 # use localhost para ambiente local
PORT=5050                      # porta padrão local (altere se conflito)

# -----------------------------
# 🧠 Application Metadata
# -----------------------------
APP_NAME=ESCALA360
APP_VERSION=1.0.0
AUTHOR=Anderson de Matos Guimarães

# -----------------------------
# 📊 Plotly / BI Configuration
# -----------------------------
PLOTLY_THEME=plotly_dark       # tema padrão do dashboard
ENABLE_BI=true                 # ativa o painel BI interativo

# -----------------------------
# 📬 Integration & Notifications (futuras)
# -----------------------------
ENABLE_EMAIL=false             # ativa envio de e-mails (futuro)
ENABLE_WHATSAPP=false          # ativa integração WhatsApp API (futuro)

# -----------------------------
# 🧰 Logging and Monitoring
# -----------------------------
LOG_LEVEL=INFO                 # níveis: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_FILE=logs/escala360.log    # caminho padrão do arquivo de log

# -----------------------------
# 💾 Database Configuration
# -----------------------------
DB_ENGINE=sqlite               # opções: sqlite | mysql | postgresql
DB_NAME=escala360.db           # nome do arquivo do banco local
DB_USER=                       # (para MySQL/PostgreSQL)
DB_PASSWORD=                   # (para MySQL/PostgreSQL)
DB_HOST=                       # (para MySQL/PostgreSQL)
DB_PORT=                       # (para MySQL/PostgreSQL)

# -----------------------------
# ⚙️ Initial Data (setup)
# -----------------------------
SQL_INIT_FILE=escala360.sql    # arquivo SQL inicial com dados básicos
