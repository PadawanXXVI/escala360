# ===========================================
# ⚙️ Exemplo de arquivo de configuração - Escala360
# ===========================================
# Este arquivo serve apenas como modelo.
# Crie um arquivo chamado `.env` na raiz do projeto
# e preencha os valores reais conforme o seu ambiente.
#
# ⚠️ Importante:
# - Nunca suba o arquivo `.env` real para o GitHub.
# - A Vercel utiliza as mesmas variáveis (via painel).
# ===========================================

# -----------------------------
# 🔧 Configurações Flask
# -----------------------------
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=sua_chave_segura_aqui
FLASK_RUN_PORT=5050   # Porta personalizada (evita conflito com outro Flask)

# -----------------------------
# 🗄️ Configurações PostgreSQL - Ambiente Local
# -----------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=escala360
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456   # Senha padrão local (ajuste conforme seu PostgreSQL)

# -----------------------------
# ⚙️ SQLAlchemy - Ambiente Local
# -----------------------------
SQLALCHEMY_DATABASE_URI=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
SQLALCHEMY_TRACK_MODIFICATIONS=False
SQLALCHEMY_ECHO=False

# -----------------------------
# 🌐 Banco de Produção (Neon / Vercel)
# -----------------------------
# Esta variável é usada no deploy em produção.
# Substitua os valores de exemplo pela string do Neon.
# Exemplo:
# DATABASE_URL=postgresql+psycopg2://usuario:senha@host:5432/banco?sslmode=require&channel_binding=require
DATABASE_URL=
