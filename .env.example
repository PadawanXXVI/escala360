# ===========================================
# ⚙️ Exemplo de arquivo de configuração - Escala360
# ===========================================
# Este arquivo serve apenas como modelo para o ambiente local.
# Crie um arquivo chamado `.env` na raiz do projeto
# e preencha os valores reais conforme o seu ambiente.
#
# 📌 Na Vercel, estas variáveis devem ser criadas manualmente
# no painel do projeto (Settings → Environment Variables).
# ===========================================

# -----------------------------
# 🔧 Configurações Flask
# -----------------------------
FLASK_APP=app.py
FLASK_ENV=development
SECRET_KEY=sua_chave_segura_aqui
FLASK_RUN_PORT=5050  # Porta alternativa recomendada (evita conflito local)

# -----------------------------
# 🗄️ Configurações PostgreSQL
# -----------------------------
# 🔹 Para uso LOCAL:
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=escala360
POSTGRES_USER=postgres
POSTGRES_PASSWORD=123456

# 🔹 Para uso em PRODUÇÃO (Vercel/Neon):
# A Vercel utiliza DATABASE_URL (e não POSTGRES_* separadamente)
# Exemplo de formato aceito:
# DATABASE_URL=postgresql+psycopg2://usuario:senha@host:5432/escala360?sslmode=require
DATABASE_URL=

# -----------------------------
# ⚙️ SQLAlchemy
# -----------------------------
SQLALCHEMY_TRACK_MODIFICATIONS=False
SQLALCHEMY_ECHO=False
